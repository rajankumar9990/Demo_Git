package com.healthcare.pages;

import com.healthcare.utils.DriverFactory;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class ProgramsPage {
    private WebDriver driver;

    // Locators (update according to actual site)
    private By programMenu = By.linkText("Programs"); 
    private By pageHeading = By.tagName("h1");

    public ProgramsPage() {
        this.driver = DriverFactory.getDriver();
    }

    // Navigate to Programs page
    public void goToPrograms() {
        driver.findElement(programMenu).click();
    }

    // Get heading text
    public String getHeading() {
        return driver.findElement(pageHeading).getText();
    }

    // Check if Programs page is opened
    public boolean isAtProgramsPage() {
        return getHeading().toLowerCase().contains("program");
    }
}
